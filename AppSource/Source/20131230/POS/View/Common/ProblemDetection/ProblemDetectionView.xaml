<UserControl x:Class="CPC.POS.View.ProblemDetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Microsoft_Windows_Controls_Ribbon="clr-namespace:Microsoft.Windows.Controls.Ribbon;assembly=RibbonControlsLibrary"
             xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna"
             xmlns:model="clr-namespace:CPC.POS.Model"
             x:Name="problemDetectionView"
             Width="300"
             Height="300"
             mc:Ignorable="d">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type model:ProblemDetectionModel}"
                                  ItemsSource="{Binding AssociateCollection, Mode=OneWay}">
            <StackPanel Orientation="Horizontal">
                <Rectangle Width="10"
                           Height="10"
                           Fill="{DynamicResource Warning}" />
                <TextBlock Style="{DynamicResource TreeviewItemTextBlock}"
                           Text="{Binding Text, Mode=OneWay}" />
            </StackPanel>
        </HierarchicalDataTemplate>
        <DataTemplate DataType="{x:Type model:ItemModel}">
            <StackPanel Orientation="Horizontal">
                <Rectangle Width="10"
                           Height="10"
                           Fill="{DynamicResource Document}" />
                <TextBlock Style="{DynamicResource TreeviewItemTextBlock}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat=" {0} {1} ">
                            <Binding Path="DataContext.Content"
                                     RelativeSource="{RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}" />
                            <Binding Path="Text" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="grdProblemDetection"
          d:LayoutOverrides="Height">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Border x:Name="brdContentProblemDetection"
                Margin="15,20,15,15"
                Background="{DynamicResource TabControlBackground}"
                BorderBrush="{DynamicResource TabControlBorder}"
                BorderThickness="1">
            <Grid x:Name="grdContentProblemDetection"
                  Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel x:Name="stkpNoItem"
                            Margin="0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            d:LayoutOverrides="Height">
                    <TextBlock x:Name="txtblItem"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               Style="{DynamicResource TextBlockNormal}"
                               Text="{Binding Message}"
                               TextWrapping="NoWrap"
                               d:LayoutOverrides="Width" />
                </StackPanel>
                <TreeView x:Name="tvProblemDetection"
                          Grid.Row="1"
                          Background="#00000000"
                          BorderBrush="#00000000"
                          ItemsSource="{Binding ProblemDetectionCollection}" />
            </Grid>
        </Border>
        <Rectangle x:Name="Line"
                   Grid.Row="1"
                   Height="1"
                   Margin="2,0,2,0"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Top"
                   Fill="{DynamicResource Spilter_Line_Background}" />
        <StackPanel x:Name="stkpBtn"
                    Grid.Row="1"
                    Height="Auto"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
            <Button x:Name="btnOK"
                    Command="{Binding OKCommand}"
                    Width="80"
                    Height="28"
                    IsDefault="True">
                <StackPanel x:Name="stkpBtnOK"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                    <Rectangle x:Name="imgOK"
                               Width="20"
                               Height="20"
                               Fill="{DynamicResource OK}" />
                    <TextBlock x:Name="txtblOK"
                               Margin="5,0,0,0"
                               VerticalAlignment="Center"
                               Style="{DynamicResource ButtonTextBlock}"
                               Text="OK"
                               TextWrapping="NoWrap" />
                </StackPanel>
            </Button>
            <!--
                <Button x:Name="btnCancel"
                Height="28"
                Width="80"
                Margin="10,0,0,0">
                <StackPanel HorizontalAlignment="Center"
                x:Name="stkpBtnCancel"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Rectangle x:Name="imgCancel"
                Fill="{DynamicResource Cancel}"
                Height="20"
                Width="20" />
                <TextBlock Margin="5,0,0,0"
                x:Name="txtblExit"
                VerticalAlignment="Center"
                Text="Cancel"
                TextWrapping="NoWrap"
                Style="{DynamicResource ButtonTextBlock}" />
                </StackPanel>
                </Button>
            -->
        </StackPanel>
    </Grid>
</UserControl>