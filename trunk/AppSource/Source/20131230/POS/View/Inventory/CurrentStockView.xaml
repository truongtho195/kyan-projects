<UserControl x:Class="CPC.POS.View.CurrentStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behavior="clr-namespace:CPC.Toolkit.Behavior"
             xmlns:control="clr-namespace:Xceed.Wpf.Toolkit;assembly=WPFToolkit.Extended"
             xmlns:converter="clr-namespace:CPC.Converter"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dataGridControl="clr-namespace:CPCToolkitExt.DataGridControl;assembly=CPCToolkitExt"
             xmlns:helper="clr-namespace:CPC.Helper"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:CPC.POS"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:textBoxControl="clr-namespace:CPCToolkitExt.TextBoxControl;assembly=CPCToolkitExt"
             x:Name="UserControl"
             Background="{DynamicResource UserControlBackground}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converter:EnumToBoolConverter x:Key="enumToBoolConverter" />
        <converter:IntToProductStoreConverter x:Key="intToProductStoreConverter" />
        <DataTemplate x:Key="ItemCodeHeaderTemplate">
            <TextBlock Text="{DynamicResource C114}" />
        </DataTemplate>
        <DataTemplate x:Key="ItemNameHeaderTemplate">
            <TextBlock Text="{DynamicResource C115}" />
        </DataTemplate>
        <DataTemplate x:Key="DescriptionHeaderTemplate">
            <TextBlock Text="{DynamicResource HolidayView_DatagridDate_Description}" />
        </DataTemplate>
        <DataTemplate x:Key="AttributeHeaderTemplate">
            <TextBlock Text="{DynamicResource C116}" />
        </DataTemplate>
        <DataTemplate x:Key="SizeHeaderTemplate">
            <TextBlock Text="{DynamicResource C117}" />
        </DataTemplate>
        <DataTemplate x:Key="BarCodeHeaderTemplate">
            <TextBlock Text="{DynamicResource RW_TextBlock_BarCode}" />
        </DataTemplate>
        <DataTemplate x:Key="PartNoHeaderTemplate">
            <TextBlock Text="{DynamicResource RW_TextBlock_PartNo}" />
        </DataTemplate>
        <DataTemplate x:Key="VendorHeaderTemplate">
            <TextBlock Text="{DynamicResource C134}" />
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate_PD_Header_Category">
            <TextBlock Text="{DynamicResource PD_Category}" />
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate_PD_Header_BaseUOM">
            <TextBlock Text="{DynamicResource C118}" />
        </DataTemplate>
        <DataTemplate x:Key="ALUHeaderTemplate">
            <TextBlock Text="{DynamicResource PD_ALU}" />
        </DataTemplate>
        <DataTemplate x:Key="DataTemplateRowDetailTemplateCurrentStock">
            <Grid x:Name="Layout1"
                  Width="Auto"
                  Margin="0"
                  VerticalAlignment="Top">
                <ScrollViewer x:Name="svRowDetailOTTemplate"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              VerticalScrollBarVisibility="Disabled">
                    <Grid x:Name="grdOTTemplate"
                          Background="{DynamicResource Datagrid_RowDetailTemplate_Background1}">
                        <Border x:Name="brdOTrTemplate"
                                Margin="15"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                BorderBrush="White"
                                BorderThickness="1"
                                d:LayoutOverrides="GridBox">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,0"
                                                     EndPoint="0.5,1">
                                    <GradientStop Offset="1"
                                                  Color="#FF262626" />
                                    <GradientStop Offset="0.541"
                                                  Color="#FF333333" />
                                    <GradientStop Offset="0.004"
                                                  Color="Black" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid x:Name="grdTemplateCustomer"
                                  Height="120">
                                <ListBox Background="{x:Null}"
                                         BorderBrush="{x:Null}"
                                         BorderThickness="0"
                                         ItemContainerStyle="{DynamicResource ListBoxItemStoreStyle}"
                                         ItemsSource="{Binding Path=ProductStoreCollection, Mode=OneWay}" />
                            </Grid>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </DataTemplate>
        <Style x:Key="ListBoxItemStoreStyle"
               TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Height"
                    Value="25" />
            <Setter Property="HorizontalContentAlignment"
                    Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="VerticalContentAlignment"
                    Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="Padding"
                    Value="2,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="true">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           Text="-"
                                           TextWrapping="Wrap" />
                                <TextBlock Margin="8,0,0,0"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource Datagrid_RowDetailTemplate_Foreground}"
                                           TextWrapping="Wrap">
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource intToProductStoreConverter}">
                                            <Binding Path="StoreCode" />
                                            <Binding Path="DataContext.StoreCollection"
                                                     ElementName="UserControl" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                                <TextBlock Grid.Column="1"
                                           Margin="4,0,0,0"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource Datagrid_RowDetailTemplate_Foreground}"
                                           Text="{Binding QuantityOnHand, StringFormat={x:Static local:Define.NumericFormat}, ConverterCulture={x:Static local:Define.ConverterCulture}}"
                                           TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="true">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"
                                               Value="true" />
                                    <Condition Property="Selector.IsSelectionActive"
                                               Value="false" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled"
                                     Value="false">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Command="{Binding HelpCommand}"
                    Gesture="F1" />
    </UserControl.InputBindings>
    <Grid x:Name="grdCurrentStock">
        <Rectangle x:Name="UserControl_Background_Image"
                   Fill="{DynamicResource UserControl_Background_Image}" />
        <control:BusyIndicator IsBusy="{Binding IsBusy}">
            <Grid x:Name="grdContentCurrentStock"
                  Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="77" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <GroupBox x:Name="gbSearch">
                    <GroupBox.Header>
                        <TextBlock x:Name="txtblSearch"
                                   Style="{DynamicResource GroupboxTextBlock}"
                                   Text="{DynamicResource TimeClockManualEventEditing_gbSearch_HeaderSearch}"
                                   TextWrapping="NoWrap" />
                    </GroupBox.Header>
                    <Grid x:Name="grdgbSearch"
                          Margin="0,4,0,0">
                        <Grid x:Name="grdgbContentSearch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <control:WatermarkTextBox x:Name="txtKeyword"
                                                      Margin="10,2"
                                                      VerticalContentAlignment="Center"
                                                      BorderBrush="{DynamicResource TextBoxCanlendarComboboxBorderBrush}"
                                                      Padding="24,0,2,0"
                                                      Text="{Binding Keyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      TextWrapping="NoWrap"
                                                      Watermark="Keyword">
                                <i:Interaction.Behaviors>
                                    <behavior:AutoFocusBehavior />
                                </i:Interaction.Behaviors>
                                <control:WatermarkTextBox.InputBindings>
                                    <KeyBinding Key="Enter"
                                                Command="{Binding SearchCommand}"
                                                CommandParameter="{Binding Path=Text, ElementName=txtKeyword}" />
                                </control:WatermarkTextBox.InputBindings>
                            </control:WatermarkTextBox>
                            <Rectangle x:Name="imgFind"
                                       Width="20"
                                       Margin="13,3,0,3"
                                       HorizontalAlignment="Left"
                                       Fill="{DynamicResource Find}" />

                        </Grid>
                    </Grid>
                </GroupBox>
                <dataGridControl:DataGridControl x:Name="dtgrdCurrentStock"
                                                 Command="{Binding LoadStepCommand}"
                                                 Grid.Row="1"
                                                 Margin="0,10.2,0,0"
                                                 AutoGenerateColumns="False"
                                                 behavior:DoubleClickCommandBehavior.DoubleClickCommand="{Binding DoubleClickViewCommand}"
                                                 behavior:DoubleClickCommandBehavior.DoubleClickCommandParameter="{Binding SelectedItem, ElementName=dtgrdCurrentStock}"
                                                 CanUserAddRows="False"
                                                 CanUserDeleteRows="False"
                                                 CurrentColumnCollection="{Binding ColumnCollection, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                                                 CurrentPageIndex="{Binding CurrentPageIndex, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                                                 d:IsHidden="True"
                                                 DisplayItems="{Binding NumberOfDisplayItems, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                                 IsColunmHidden="True"
                                                 IsPaging="True"
                                                 IsRollBackData="False"
                                                 ItemsSource="{Binding ProductCollection}"
                                                 NumberOfItems="{Binding TotalProducts, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                                 RowDetailsTemplate="{DynamicResource DataTemplateRowDetailTemplateCurrentStock}"
                                                 RowStyle="{DynamicResource DataGridToggleRowStyle}"
                                                 SelectedIndex="0"
                                                 SelectedItem="{Binding SelectedProduct}"
                                                 Style="{DynamicResource DatagridWithRowHeader}">
                    <dataGridControl:DataGridControl.Columns>
                        <DataGridTemplateColumn Width="25"
                                                MinWidth="25"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.None}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ToggleButton x:Name="TgPlus"
                                                  IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Style="{DynamicResource PlusMinusToggleButtonStyle}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="95"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Code}"
                                                HeaderTemplate="{StaticResource ItemCodeHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Code, Mode=OneWay}"
                                               TextAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="150"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Category}"
                                                HeaderTemplate="{StaticResource DataTemplate_PD_Header_Category}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Left"
                                               Text="{Binding CategoryName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="150"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Vendor}"
                                                HeaderTemplate="{StaticResource VendorHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Left"
                                               Text="{Binding VendorName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="100"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.ItemName}"
                                                HeaderTemplate="{StaticResource ItemNameHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ProductName, Mode=OneWay}"
                                               TextAlignment="Left" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Binding="{Binding Description}"
                                            Width="150"
                                            dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Description}"
                                            HeaderTemplate="{StaticResource DescriptionHeaderTemplate}"
                                            IsReadOnly="True" />
                        <DataGridTemplateColumn Width="80"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Attribute}"
                                                HeaderTemplate="{StaticResource AttributeHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Attribute, Mode=OneWay}"
                                               TextAlignment="Left" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="80"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Size}"
                                                HeaderTemplate="{StaticResource SizeHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Size, Mode=OneWay}"
                                               TextAlignment="Left" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="100"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.Barcode}"
                                                HeaderTemplate="{StaticResource BarCodeHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Barcode, Mode=OneWay}"
                                               TextAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="100"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.PartNumber}"
                                                HeaderTemplate="{StaticResource PartNoHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PartNumber, Mode=OneWay}"
                                               TextAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="100"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.ALU}"
                                                HeaderTemplate="{StaticResource ALUHeaderTemplate}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ALU, Mode=OneWay}"
                                               TextAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="80"
                                                dataGridControl:DataGridColumnExtensions.Name="{x:Static local:SearchOptions.UOM}"
                                                HeaderTemplate="{StaticResource DataTemplate_PD_Header_BaseUOM}"
                                                IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Left"
                                               Text="{Binding UOMName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </dataGridControl:DataGridControl.Columns>
                </dataGridControl:DataGridControl>
            </Grid>
        </control:BusyIndicator>
    </Grid>
</UserControl>