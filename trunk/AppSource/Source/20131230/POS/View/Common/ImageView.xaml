<UserControl x:Class="CPC.POS.View.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:control="clr-namespace:CPC.Control"
             xmlns:c="clr-namespace:CPC.Control.Converters"
             mc:Ignorable="d">

    <UserControl.Resources>
        <c:PathToBitmapImageConverter x:Key="PathToBitmapImageConverter" />
    </UserControl.Resources>

    <Grid>
        <control:ImageViewer Source="{Binding DataContext.FilePath, Mode=OneWay, Converter={StaticResource PathToBitmapImageConverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}" />
    </Grid>

</UserControl>
