//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections.Generic;
using System.Data.Objects;
using System.Linq;
using System.Linq.Expressions;
using CPC.POSReport.Database;

namespace CPC.POSReport.Repository
{
    /// <summary>
    /// Repository for table base_CostAdjustment 
    /// </summary>
    public partial class base_CostAdjustmentRepository
    {
        #region Auto Generate Code

        #region Constructors

        // Default constructor
        public base_CostAdjustmentRepository()
        {
        }

        #endregion

        #region Basic C.R.U.D. Operations

        /// <summary>
        /// Add new base_CostAdjustment.
        /// </summary>
        /// <param name="base_CostAdjustment">base_CostAdjustment to add.</param>
        /// <returns>base_CostAdjustment have been added.</returns>
        public base_CostAdjustment Add(base_CostAdjustment base_CostAdjustment)
        {
            UnitOfWork.Add<base_CostAdjustment>(base_CostAdjustment);
            return base_CostAdjustment;
        }

        /// <summary>
        /// Adds a sequence of new base_CostAdjustment.
        /// </summary>
        /// <param name="base_CostAdjustment">Sequence of new base_CostAdjustment to add.</param>
        /// <returns>Sequence of new base_CostAdjustment have been added.</returns>
        public IEnumerable<base_CostAdjustment> Add(IEnumerable<base_CostAdjustment> base_CostAdjustment)
        {
            UnitOfWork.Add<base_CostAdjustment>(base_CostAdjustment);
            return base_CostAdjustment;
        }

        /// <summary>
        /// Delete a existed base_CostAdjustment.
        /// </summary>
        /// <param name="base_CostAdjustment">base_CostAdjustment to delete.</param>
        public void Delete(base_CostAdjustment base_CostAdjustment)
        {
            Refresh(base_CostAdjustment);
            if (base_CostAdjustment.EntityState != System.Data.EntityState.Detached)
                UnitOfWork.Delete<base_CostAdjustment>(base_CostAdjustment);
        }

        /// <summary>
        /// Delete a sequence of existed base_CostAdjustment.
        /// </summary>
        /// <param name="base_CostAdjustment">Sequence of existed base_CostAdjustment to delete.</param>
        public void Delete(IEnumerable<base_CostAdjustment> base_CostAdjustment)
        {
            int total = base_CostAdjustment.Count();
            for (int i = total - 1; i >= 0; i--)
                Delete(base_CostAdjustment.ElementAt(i));
        }

        /// <summary>
        /// Returns the first base_CostAdjustment of a sequence that satisfies a specified condition or 
        /// a default value if no such base_CostAdjustment is found.
        /// </summary>
        /// <param name="expression">A function to test each base_CostAdjustment for a condition.</param>
        /// <returns>    
        /// Null if source is empty or if no base_CostAdjustment passes the test specified by expression; 
        /// otherwise, the first base_CostAdjustment in source that passes the test specified by expression.
        /// </returns>
        public base_CostAdjustment Get(Expression<Func<base_CostAdjustment, bool>> expression)
        {
            return UnitOfWork.Get<base_CostAdjustment>(expression);
        }

        /// <summary>
        /// Get all base_CostAdjustment.
        /// </summary>
        /// <returns>The new IList&lt;base_CostAdjustment&gt; instance.</returns>
        public IList<base_CostAdjustment> GetAll()
        {
            return UnitOfWork.GetAll<base_CostAdjustment>().ToList();
        }

        /// <summary>
        /// Get all base_CostAdjustment that satisfies a specified condition.
        /// </summary>
        /// <param name="expression">A function to test each base_CostAdjustment for a condition.</param>
        /// <returns>The new IList&lt;base_CostAdjustment&gt; instance.</returns>
        public IList<base_CostAdjustment> GetAll(Expression<Func<base_CostAdjustment, bool>> expression)
        {
            return UnitOfWork.GetAll<base_CostAdjustment>(expression).ToList();
        }

        /// <summary>
        /// Get all base_CostAdjustment.
        /// </summary>
        /// <returns>The new IEnumerable&lt;base_CostAdjustment&gt; instance.</returns>
        public IEnumerable<base_CostAdjustment> GetIEnumerable()
        {
            return UnitOfWork.GetIEnumerable<base_CostAdjustment>();
        }

        /// <summary>
        /// Get all base_CostAdjustment that satisfies a specified condition.
        /// </summary>
        /// <param name="expression">A function to test each base_CostAdjustment for a condition.</param>
        /// <returns>The new IEnumerable&lt;base_CostAdjustment&gt; instance.</returns>
        public IEnumerable<base_CostAdjustment> GetIEnumerable(Expression<Func<base_CostAdjustment, bool>> expression)
        {
            return UnitOfWork.GetIEnumerable<base_CostAdjustment>(expression);
        }

        /// <summary>
        /// Get all base_CostAdjustment.
        /// </summary>
        /// <returns>The new IQueryable&lt;base_CostAdjustment&gt; instance.</returns>
        public IQueryable<base_CostAdjustment> GetIQueryable()
        {
            return UnitOfWork.GetIQueryable<base_CostAdjustment>();
        }

        /// <summary>
        /// Get all base_CostAdjustment that satisfies a specified condition.
        /// </summary>
        /// <param name="expression">A function to test each base_CostAdjustment for a condition.</param>
        /// <returns>The new IQueryable&lt;base_CostAdjustment&gt; instance.</returns>
        public IQueryable<base_CostAdjustment> GetIQueryable(Expression<Func<base_CostAdjustment, bool>> expression)
        {
            return UnitOfWork.GetIQueryable<base_CostAdjustment>(expression);
        }

        /// <summary>
        /// Take a few base_CostAdjustment in a sequence was sorted on server.
        /// </summary>
        /// <param name="ignoreCount">Number of base_CostAdjustment will ignore.</param>
        /// <param name="takeCount">Number of base_CostAdjustment will take.</param>
        /// <param name="keys">The key columns by which to order the results.</param>
        /// <returns>The new IList&lt;base_CostAdjustment&gt; instance.</returns>
        public IList<base_CostAdjustment> GetRange(int ignoreCount, int takeCount, string keys)
        {
            return UnitOfWork.GetRange<base_CostAdjustment>(ignoreCount, takeCount, keys);
        }

        /// <summary>
        /// Take a few base_CostAdjustment in a sequence was sorted on server.
        /// </summary>
        /// <param name="ignoreCount">Number of base_CostAdjustment will ignore.</param>
        /// <param name="takeCount">Number of base_CostAdjustment will take.</param>
        /// <param name="keys">The key columns by which to order the results.</param>
        /// <param name="expression">A function to test each base_CostAdjustment for a condition.</param>
        /// <returns>The new IList&lt;base_CostAdjustment&gt; instance.</returns>
        public IList<base_CostAdjustment> GetRange(int ignoreCount, int takeCount, string keys, Expression<Func<base_CostAdjustment, bool>> expression)
        {
            return UnitOfWork.GetRange<base_CostAdjustment>(ignoreCount, takeCount, keys, expression);
        }

        /// <summary>
        /// Updates an base_CostAdjustment in the object context with data from the data source.
        /// </summary>
        /// <param name="base_CostAdjustment">The base_CostAdjustment to be refreshed.</param>
        public base_CostAdjustment Refresh(base_CostAdjustment base_CostAdjustment)
        {
            UnitOfWork.Refresh<base_CostAdjustment>(base_CostAdjustment);
            if (base_CostAdjustment.EntityState != System.Data.EntityState.Detached)
                return base_CostAdjustment;
            return null;
        }

        /// <summary>
        /// Updates a sequence of base_CostAdjustment in the object context with data from the data source.
        /// </summary>
        /// <typeparam name="base_CostAdjustment">Type of object in a sequence to refresh.</typeparam>
        /// <param name="base_CostAdjustment">Object collection to be refreshed.</param>
        public void Refresh(IEnumerable<base_CostAdjustment> base_CostAdjustment)
        {
            UnitOfWork.Refresh<base_CostAdjustment>(base_CostAdjustment);
        }

        /// <summary>
        /// Updates a sequence of base_CostAdjustment in the object context with data from the data source.
        /// </summary>
        public void Refresh()
        {
            UnitOfWork.Refresh<base_CostAdjustment>();
        }

        /// <summary>
        /// Persists all updates to the data source and resets change tracking in the object context.
        /// </summary>
        public void Commit()
        {
            UnitOfWork.Commit();
        }

        /// <summary>
        /// Persists all updates to the data source with the specified System.Data.Objects.SaveOptions.
        /// </summary>
        /// <param name="options">A System.Data.Objects.SaveOptions value that determines the behavior of the operation.</param>
        public void Commit(SaveOptions options)
        {
            UnitOfWork.Commit(options);
        }

        /// <summary>
        /// Starts a database transaction.
        /// </summary>
        public void BeginTransaction()
        {
            UnitOfWork.BeginTransaction();
        }

        /// <summary>
        /// Commits the database transaction.
        /// </summary>
        public void CommitTransaction()
        {
            UnitOfWork.CommitTransaction();
        }

        /// <summary>
        /// Rolls back a transaction from a pending state.
        /// </summary>
        public void RollbackTransaction()
        {
            UnitOfWork.RollbackTransaction();
        }

        #endregion

        #endregion

        #region Custom Code


        #endregion
    }
}
