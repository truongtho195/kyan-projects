<Window x:Class="FlashCard.Views.TestScrollView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestScrollView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="132*" />
            <RowDefinition Height="179*" />
        </Grid.RowDefinitions>
        <FlowDocumentScrollViewer  Margin="0,0,73,7"
                                   Document="{Binding SelectedLesson.Description,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"/>
        <FlowDocumentScrollViewer  Margin="0,15,73,39"
                                   Grid.Row="1"
                                   Document="{Binding SelectedLesson.BackSideModel.BackSideDetail,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
</Window>
